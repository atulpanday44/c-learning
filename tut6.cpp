#include<iostream>
using namespace std;

int main()
{
    int a=4,b=5;
    cout<<"operator in c++:"<<endl;
    cout<<"Following are the types of operators in c++"<<endl;
    //Arithmatic operator
    cout<<"the value of a+b is "<<a+b<<endl;
    cout<<"the value of a-b is "<<a-b<<endl;
    cout<<"the value of a*b is "<<a*b<<endl;
    cout<<"the value of a/b is "<<a/b<<endl;
    cout<<"the value of a%b is "<<a%b<<endl;
    cout<<"the value of a++ is "<<a++<<endl;
    cout<<"the value of a-- is "<<a--<<endl;
    cout<<"the value of ++a is "<<++a<<endl;
    cout<<"the value of --a is "<<--a<<endl;
    cout<<"This is Hellow World program"<<endl;
    // this is assignment operator
   cout<<"following are the type of comparision operator"<<endl;

    cout<<"the value of a == b is "<<(a==b)<<endl;   
    cout<<"the value of a != b is "<<(a!=b)<<endl; 
    cout<<"the value of a >= b is "<<(a>=b)<<endl; 
    cout<<"the value of a <= b is "<<(a<=b)<<endl; 
    cout<<"the value of a<b is "<<(a<b)<<endl;
    cout<<"the value of a>b is "<<(a>b)<<endl;  
    // Logical operator
    cout<<"following are the logical operator in c++"<<endl;
    cout<<"the value of the logical AND operator ((a==b)) && (a<b)) is :"<<((a==b)) && (a<b))
    <<endl;
    cout<<"the value of the  logical OR operator ((a==b) || (a<b)) is :"<<((a==b)) || (a<b))
    <<endl;
    cout<<"the value of the  logical NOT operator(!(a==b)) is :"<<(!(a==b))<<endl;
    return 0;
}

